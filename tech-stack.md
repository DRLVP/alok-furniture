Vite + React 19          → Core framework (perfect choice)
Tailwind CSS v4          → Styling (navy/gold theme via CSS vars)
React Three Fiber        → 3D furniture models in hero
@react-three/drei        → R3F helper utilities
GSAP + ScrollTrigger     → Scroll animations
Framer Motion            → UI micro-animations
React Router v7          → Page routing
React Hook Form          → Enquiry / booking forms
Zustand                  → State (EMI calculator, filters)
EmailJS or Nodemailer    → Contact form emails
React-Whatsapp-Widget    → Float button
Google Maps React        → Store locator